"use client";

import { FormEvent, useCallback, useEffect, useMemo, useState } from "react";
import { ArrowRight, Building2, Edit3, Mail, MapPin, MessageSquarePlus, Phone, Plus, Search, UsersRound, X } from "lucide-react";
import { usePathname, useRouter } from "next/navigation";
import { adminFetch } from "@/lib/auth/admin-client";
import type { CustomerNote, CustomerRecord, CustomerStatus, CustomerType } from "@/lib/db/types";

const customerTypes: Array<{ value: CustomerType; label: string }> = [
  { value: "hvac_contractor", label: "HVAC contractor" }, { value: "consultant", label: "Consultant" }, { value: "peb_contractor", label: "PEB contractor" }, { value: "architect", label: "Architect" }, { value: "dealer", label: "Dealer" }, { value: "end_user", label: "End user" }, { value: "industrial_customer", label: "Industrial customer" }, { value: "other", label: "Other" },
];
const statuses: CustomerStatus[] = ["active", "inactive", "archived"];
type CustomerDetail = { customer: CustomerRecord; notes: CustomerNote[]; linked: { enquiries: number; quotations: number; projects: number } };
type CustomerDraft = Omit<CustomerRecord, "id" | "createdAt">;
const typeLabel = (value: CustomerType) => customerTypes.find((item) => item.value === value)?.label || value;

export default function AdminCustomersPanel() {
  const pathname = usePathname(); const router = useRouter();
  const [records, setRecords] = useState<CustomerRecord[]>([]); const [query, setQuery] = useState(""); const [status, setStatus] = useState("all"); const [selected, setSelected] = useState<CustomerDetail | null>(null); const [compose, setCompose] = useState(false); const [loading, setLoading] = useState(true); const [message, setMessage] = useState("");
  const load = useCallback(async () => { const response = await adminFetch("/api/admin/customers"); const data = await response.json() as { customers?: CustomerRecord[]; message?: string }; setRecords(data.customers || []); if (!response.ok) setMessage(data.message || "Could not load customers."); setLoading(false); }, []);
  useEffect(() => { const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, [load]);
  useEffect(() => { if (pathname.endsWith("/new")) router.replace("/admin/account-approvals"); }, [pathname, router]);
  const filtered = useMemo(() => records.filter((record) => (status === "all" || record.status === status) && [record.fullName, record.company, record.phone, record.email, record.gstin, record.city].join(" ").toLowerCase().includes(query.toLowerCase())), [records, query, status]);
  const open = async (id: string) => { const response = await adminFetch(`/api/admin/customers/${id}`); const data = await response.json() as { customer?: CustomerRecord; notes?: CustomerNote[]; linked?: CustomerDetail["linked"]; message?: string }; if (!response.ok || !data.customer || !data.linked) { setMessage(data.message || "Could not open this customer."); return; } setSelected({ customer: data.customer, notes: data.notes || [], linked: data.linked }); };
  const closeCompose = () => { setCompose(false); if (pathname.endsWith("/new")) router.replace("/admin/customers"); };
  const create = async (draft: CustomerDraft) => { const response = await adminFetch("/api/admin/customers", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(draft) }); const data = await response.json() as { customer?: CustomerRecord; message?: string }; if (!response.ok || !data.customer) { setMessage(data.message || "Could not create the customer."); return false; } setRecords((current) => [data.customer!, ...current]); setMessage("Customer created."); closeCompose(); return true; };
  const update = async (patch: Partial<CustomerDraft>) => { if (!selected) return false; const response = await adminFetch(`/api/admin/customers/${selected.customer.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(patch) }); const data = await response.json() as { customer?: CustomerRecord; message?: string }; if (!response.ok || !data.customer) { setMessage(data.message || "Could not update the customer."); return false; } setSelected((current) => current ? { ...current, customer: data.customer! } : current); setRecords((current) => current.map((item) => item.id === data.customer!.id ? data.customer! : item)); setMessage("Customer updated."); return true; };
  const addNote = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!selected) return; const form = new FormData(event.currentTarget); const note = String(form.get("note") || "").trim(); if (!note) return; const response = await adminFetch(`/api/admin/customers/${selected.customer.id}/notes`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ note }) }); const data = await response.json() as { note?: CustomerNote; message?: string }; if (!response.ok || !data.note) { setMessage(data.message || "Could not add the note."); return; } setSelected((current) => current ? { ...current, notes: [data.note!, ...current.notes] } : current); event.currentTarget.reset(); setMessage("Internal note added."); };
  return <div className="admin-records"><section className="admin-records-toolbar"><div className="admin-records-search"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search customer, company, phone, email or GSTIN" /></div><select value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">All statuses</option>{statuses.map((item) => <option key={item} value={item}>{item}</option>)}</select><button type="button" onClick={() => router.push("/admin/account-approvals")}><Plus size={14} />Review approvals</button><button type="button" onClick={() => { setLoading(true); void load(); }}>Refresh</button></section>{message && <p className="admin-records-message" role="status">{message}</p>}<div className="admin-records-table-wrap"><div className="admin-records-table admin-customers-table"><div className="admin-records-heading"><span>Created</span><span>Customer / company</span><span>Contact</span><span>Type</span><span>GSTIN</span><span>Status</span><span>Location</span><span /></div>{loading ? <div className="admin-records-empty">Loading customers...</div> : filtered.length ? filtered.map((record) => <button type="button" className="admin-records-row" key={record.id} onClick={() => void open(record.id)}><span>{new Date(record.createdAt).toLocaleDateString("en-IN")}</span><span><strong>{record.fullName}</strong><small>{record.company || "Company not provided"}</small></span><span><strong>{record.phone || "No phone"}</strong><small>{record.email || "No email"}</small></span><span>{typeLabel(record.customerType)}</span><span>{record.gstin || "-"}</span><span><em className={`admin-status ${record.status}`}>{record.status}</em></span><span>{[record.city, record.state].filter(Boolean).join(", ") || "-"}</span><ArrowRight size={16} /></button>) : <div className="admin-records-empty"><UsersRound size={25} /><strong>No matching customers</strong><p>Approved registered accounts appear here after review.</p></div>}</div></div>{compose && <CustomerForm title="Add customer" onClose={closeCompose} onSubmit={create} />}{selected && <CustomerDrawer detail={selected} onClose={() => setSelected(null)} onUpdate={update} onAddNote={addNote} />}</div>;
}

function CustomerForm({ title, initial, onClose, onSubmit }: { title: string; initial?: CustomerRecord; onClose: () => void; onSubmit: (draft: CustomerDraft) => Promise<boolean> }) {
  const [busy, setBusy] = useState(false); const [error, setError] = useState("");
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const draft: CustomerDraft = { fullName: String(form.get("fullName") || ""), company: String(form.get("company") || ""), phone: String(form.get("phone") || ""), email: String(form.get("email") || ""), gstin: String(form.get("gstin") || ""), billingAddress: String(form.get("billingAddress") || ""), shippingAddress: String(form.get("shippingAddress") || ""), city: String(form.get("city") || ""), state: String(form.get("state") || ""), pinCode: String(form.get("pinCode") || ""), customerType: String(form.get("customerType") || "other") as CustomerType, notes: String(form.get("notes") || ""), status: String(form.get("status") || "active") as CustomerStatus }; setBusy(true); setError(""); const saved = await onSubmit(draft); if (!saved) setError("Check the customer details. A matching phone, email or GSTIN may already exist."); setBusy(false); };
  return <div className="admin-drawer-backdrop" role="presentation" onMouseDown={onClose}><aside className="admin-enquiry-drawer admin-customer-form" role="dialog" aria-modal="true" aria-label={title} onMouseDown={(event) => event.stopPropagation()}><header><div><p>CUSTOMER MASTER</p><h2>{title}</h2><span>Use a single record for enquiries, projects and quotations.</span></div><button type="button" onClick={onClose} aria-label="Close customer form"><X size={18} /></button></header><form className="admin-drawer-body admin-customer-fields" onSubmit={submit}><div className="admin-customer-fields-grid"><label>Contact name<input name="fullName" required defaultValue={initial?.fullName} /></label><label>Company<input name="company" defaultValue={initial?.company} /></label><label>Phone<input name="phone" defaultValue={initial?.phone} /></label><label>Email<input name="email" type="email" defaultValue={initial?.email} /></label><label>GSTIN<input name="gstin" defaultValue={initial?.gstin} /></label><label>Customer type<select name="customerType" defaultValue={initial?.customerType || "other"}>{customerTypes.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label>City<input name="city" defaultValue={initial?.city} /></label><label>State<input name="state" defaultValue={initial?.state} /></label><label>PIN code<input name="pinCode" defaultValue={initial?.pinCode} /></label><label>Status<select name="status" defaultValue={initial?.status || "active"}>{statuses.map((item) => <option key={item} value={item}>{item}</option>)}</select></label></div><label>Billing address<textarea name="billingAddress" defaultValue={initial?.billingAddress} /></label><label>Shipping address<textarea name="shippingAddress" defaultValue={initial?.shippingAddress} /></label><label>Internal customer notes<textarea name="notes" defaultValue={initial?.notes} /></label>{error && <p className="admin-form-error">{error}</p>}<div className="admin-customer-form-actions"><button type="button" className="admin-drawer-secondary" onClick={onClose}>Cancel</button><button className="admin-os-primary" disabled={busy}>{busy ? "Saving..." : "Save customer"}<ArrowRight size={15} /></button></div></form></aside></div>;
}

function CustomerDrawer({ detail, onClose, onUpdate, onAddNote }: { detail: CustomerDetail; onClose: () => void; onUpdate: (patch: Partial<CustomerDraft>) => Promise<boolean>; onAddNote: (event: FormEvent<HTMLFormElement>) => void }) {
  const [editing, setEditing] = useState(false); const { customer, notes, linked } = detail;
  return <div className="admin-drawer-backdrop" role="presentation" onMouseDown={onClose}><aside className="admin-enquiry-drawer admin-customer-drawer" role="dialog" aria-modal="true" aria-label={customer.fullName} onMouseDown={(event) => event.stopPropagation()}><header><div><p>CUSTOMER RECORD</p><h2>{customer.fullName}</h2><span>{customer.company || "Company not provided"}</span></div><button type="button" onClick={onClose} aria-label="Close customer"><X size={18} /></button></header><div className="admin-drawer-body"><section className="admin-drawer-summary">{customer.phone && <a href={`tel:${customer.phone}`}><Phone size={15} />{customer.phone}</a>}{customer.email && <a href={`mailto:${customer.email}`}><Mail size={15} />{customer.email}</a>}<span><MapPin size={15} />{[customer.city, customer.state, customer.pinCode].filter(Boolean).join(", ") || "Location not provided"}</span></section><section className="admin-customer-linked"><div><strong>{linked.enquiries}</strong><span>Enquiries</span></div><div><strong>{linked.projects}</strong><span>Projects</span></div><div><strong>{linked.quotations}</strong><span>Quotations</span></div></section><section><h3>Customer information</h3><dl><div><dt>Type</dt><dd>{typeLabel(customer.customerType)}</dd></div><div><dt>GSTIN</dt><dd>{customer.gstin || "Not provided"}</dd></div><div><dt>Status</dt><dd><em className={`admin-status ${customer.status}`}>{customer.status}</em></dd></div></dl>{customer.notes && <p className="admin-drawer-message">{customer.notes}</p>}</section><section className="admin-drawer-controls"><h3>Record control</h3><label>Status<select value={customer.status} onChange={(event) => void onUpdate({ status: event.target.value as CustomerStatus })}>{statuses.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><button type="button" className="admin-customer-edit" onClick={() => setEditing(true)}><Edit3 size={15} />Edit customer details</button></section><section className="admin-drawer-notes"><div><h3>Internal notes</h3><span>{notes.length}</span></div><form onSubmit={onAddNote}><textarea name="note" required placeholder="Add an internal note for this customer" /><button type="submit"><MessageSquarePlus size={15} />Add note</button></form>{notes.length ? <ol>{notes.map((note) => <li key={note.id}><time>{new Date(note.createdAt).toLocaleString("en-IN", { dateStyle: "medium", timeStyle: "short" })}</time><p>{note.note}</p></li>)}</ol> : <p className="admin-drawer-muted">No internal notes yet.</p>}</section></div><footer><button type="button" className="admin-drawer-secondary" onClick={onClose}>Close</button><button type="button" className="admin-os-primary" onClick={() => setEditing(true)}><Building2 size={16} />Manage record</button></footer>{editing && <CustomerForm title="Edit customer" initial={customer} onClose={() => setEditing(false)} onSubmit={async (draft) => { const updated = await onUpdate(draft); if (updated) setEditing(false); return updated; }} />}</aside></div>;
}
