import { NextResponse } from "next/server";
import { getAdminRequestContext } from "@/lib/auth/admin-server";
import { listAdminProducts, updateAdminProduct } from "@/lib/repositories/products";
import { adminProductPatchSchema } from "@/lib/validation/admin-products";
export const dynamic = "force-dynamic";
export async function GET(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await getAdminRequestContext(request)) return NextResponse.json({ ok: false, message: "Authorised Admin access is required." }, { status: 401 }); try { const id = (await params).id; const product = (await listAdminProducts()).find((item) => item.id === id); return product ? NextResponse.json({ ok: true, product }) : NextResponse.json({ ok: false, message: "Product not found." }, { status: 404 }); } catch (error) { return NextResponse.json({ ok: false, message: error instanceof Error ? error.message : "Could not load the product." }, { status: 500 }); } }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await getAdminRequestContext(request)) return NextResponse.json({ ok: false, message: "Authorised Admin access is required." }, { status: 401 }); const parsed = adminProductPatchSchema.safeParse(await request.json()); if (!parsed.success) return NextResponse.json({ ok: false, message: parsed.error.issues[0]?.message || "Check the product update." }, { status: 400 }); try { const product = await updateAdminProduct((await params).id, parsed.data); return product ? NextResponse.json({ ok: true, product }) : NextResponse.json({ ok: false, message: "Product not found." }, { status: 404 }); } catch (error) { return NextResponse.json({ ok: false, message: error instanceof Error ? error.message : "Could not update the product." }, { status: 500 }); } }
